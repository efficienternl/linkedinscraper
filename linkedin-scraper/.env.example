# Optioneel — override standaardpaden. Niet verplicht, login gebeurt altijd handmatig.
SESSION_FILE=session.json
OUTPUT_DIR=output
HEADLESS=true

# Apify API token (optioneel, voor 'python cli.py search' via Apify)
APIFY_TOKEN=your_apify_token_here
